# Practical instance for a simple chrome extension

# Deploy/pack and run the extension/code

```bash
    $ npm run dev
```

# Dev server

```bash
    $ npm run local -- --watch
```

# Live dev server

```bash
    $ npm run local -- --watch
```
